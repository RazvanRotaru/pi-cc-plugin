{
  "name": "pi-cc-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin that delegates work to pi-subagents (nicobailon fork).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/",
    "test:smoke": "PI_SMOKE=1 node --test tests/smoke/",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  },
  "license": "MIT"
}
